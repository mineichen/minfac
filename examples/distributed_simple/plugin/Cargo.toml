[package]
name = "plugin"
version = "0.0.1"
authors = ["Markus Ineichen <kusi.ineichen@gmail.com>"]
edition = "2018"

[lib]
crate-type = ["dylib"]

[dependencies]
minfac = { path = "../../../" }
interface = { path = "../interface" } 
