[package]
name = "plugin"
version = "0.0.1"
authors = ["Markus Ineichen <kusi.ineichen@gmail.com>"]
edition = "2018"

[lib]
crate-type = ["dylib"]

[dependencies]
ioc-rs = { path = "../../" }
interface = { path = "../interface" } 
